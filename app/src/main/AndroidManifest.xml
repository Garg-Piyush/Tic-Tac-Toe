<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.onlinetic_tac_toewithblogs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BlogWithTopicActivity"></activity>
        <activity android:name=".BlogWritingActivity" />
        <activity android:name=".GameScreenActivity" />
        <activity android:name=".PlayersActivity" />
        <activity android:name=".BlogsActivity" />
        <activity android:name=".RequestsActivity" />
        <activity
            android:name=".Sign_Up_Activity"
            android:label="@string/app_name" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".BlogWithTopicActivity" />
    </application>

</manifest>